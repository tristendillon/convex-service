import { expect, test } from 'vitest'
import { convexTest } from './setup'

test('schema wrapper basic functionality', async () => {
  const t = convexTest()

  // Test will be implemented once schema wrapper is built
  expect(true).toBe(true)
})
